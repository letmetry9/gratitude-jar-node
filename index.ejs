<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>The Gratitude Jar</title>
    <style>
        /* CSS is kept internal to avoid needing a public folder */
        body {
            background-color: <%= colorString %>;
            font-family: 'Courier New', Courier, monospace;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            margin: 0;
            transition: background-color 2s ease;
            text-align: center;
        }
        .container {
            background: white;
            padding: 2rem;
            border-radius: 15px;
            box-shadow: 0 10px 25px rgba(0,0,0,0.1);
            max-width: 400px;
            width: 90%;
        }
        h1 { margin-bottom: 0.5rem; }
        p.counter { color: #666; font-size: 0.9rem; margin-bottom: 2rem; }
        textarea {
            width: 100%;
            height: 80px;
            margin-bottom: 1rem;
            padding: 10px;
            border: 2px solid #ddd;
            border-radius: 5px;
            resize: none;
            box-sizing: border-box; 
        }
        button {
            background: #333;
            color: white;
            border: none;
            padding: 10px 20px;
            cursor: pointer;
            border-radius: 5px;
            font-size: 1rem;
        }
        button:hover { background: #555; }
        .random-note {
            margin-top: 2rem;
            padding-top: 2rem;
            border-top: 1px dashed #ccc;
        }
        #display-area {
            font-style: italic;
            min-height: 50px;
            display: flex;
            align-items: center;
            justify-content: center;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>The Gratitude Jar</h1>
        <p class="counter">Total notes collected: <%= count %></p>

        <form action="/add" method="POST">
            <textarea name="note" placeholder="I am grateful for..." required></textarea>
            <button type="submit">Drop in Jar</button>
        </form>

        <div class="random-note">
            <div id="display-area">Need a boost? Click below.</div>
            <button onclick="fetchNote()" style="background: transparent; color: #333; border: 1px solid #333; margin-top: 10px;">Read a Note</button>
        </div>
    </div>

    <script>
        async function fetchNote() {
            const res = await fetch('/random');
            const data = await res.json();
            document.getElementById('display-area').innerText = `"${data.text}"`;
        }
    </script>
</body>
</html>
